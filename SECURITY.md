# Security Policy

This portfolio repository must contain fictional sample data only. Do not commit customer names, phone numbers, addresses, order details, calendar IDs, OAuth credentials, exported spreadsheets, or real business data.

Google authorization is handled by Apps Script at runtime and must never be copied into this repository.

